# تشخيص مشاكل الحفظ في Supabase

## خطوات التشخيص:

### 1️⃣ افتح أدوات المطور (Console)
- اضغط `F12` أو `Ctrl+Shift+I`
- انتقل إلى تبويب **Console**

### 2️⃣ جرّب الحفظ مرة أخرى
- سجّل الدخول
- أدخل البيانات
- اضغط **حفظ**

### 3️⃣ ابحث عن الرسائل التالية في Console:

#### ✅ إذا رأيت هذه الرسائل:
```
✅ بيانات Google Drive محدثة بنجاح
Attempting to insert record: {object}
Insert successful: {array}
```
**النتيجة**: البيانات حُفظت بنجاح! ✅

#### ❌ إذا رأيت خطأ مثل:
```
Supabase error details: {error}
Error code: PGRST116
Error message: ...
```

## 🔧 حسب نوع الخطأ:

### خطأ: `PGRST116` أو `null value in column`
**السبب**: حقل مطلوب فارغ
**الحل**: 
- تأكد من ملء جميع الحقول المطلوبة
- تحقق من أن `sale_date` موجود

### خطأ: `permission denied`
**السبب**: مشكلة في صلاحيات قاعدة البيانات (RLS)
**الحل**:
1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **SQL Editor**
4. انسخ والصق هذا الكود:
```sql
-- تعطيل RLS مؤقتاً للاختبار
ALTER TABLE public.sales DISABLE ROW LEVEL SECURITY;
```
5. إذا نجح الحفظ بعدها، فالمشكلة في الـ RLS

### خطأ: `column "..." does not exist`
**السبب**: اسم العمود غير صحيح
**الحل**:
1. اذهب إلى Supabase Dashboard
2. اختر **Sales** table
3. تحقق من أسماء الأعمدة (يجب أن تكون snake_case)

## 📋 الأعمدة المطلوبة في جدول `sales`:
- `id` (auto-increment)
- `employee_id` ✅
- `branch` ✅
- `sale_date` ✅
- `total_sales` ✅
- `total_cash` ✅
- `total_card` ✅
- `visa_sales` (اختياري)
- `master_sales` (اختياري)
- `mada_sales` (اختياري)
- `tamara` (اختياري)
- `tabby` (اختياري)
- `extra_cash` (اختياري)
- `expenses` (اختياري)
- `returns` (اختياري)
- `withdrawal` (اختياري)
- `notes` (اختياري)
- `financing_notes` (اختياري)
- `attachment` (اختياري)
- `created_at` ✅

## 🆘 إذا استمرت المشكلة:

انسخ الرسالة من Console وشارك معها:
1. **Error code**
2. **Error message**
3. **أسماء الحقول المرسلة**
